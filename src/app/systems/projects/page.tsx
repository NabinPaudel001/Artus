import React from "react";
import OurProjects from "@/partials/OurProjects"; // Adjust this import based on where you placed the file

const ProjectsPage = () => {
  return (
    <main>
      <OurProjects />
    </main>
  );
};

export default ProjectsPage;
